import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'; import Card from '@/components/ui/Card'; import useMarketSectionAggregation from '../hooks/useMarketSectionAggregation'; import type { AnalyticsDateRange } from '@/api/types/analytics.types'; import styles from '../pages/ViolationAnalyticsPage.module.css';
const MarketSectionBarChart = ({ range }: { range: AnalyticsDateRange }) => { const query = useMarketSectionAggregation(range); const data = query.data ?? []; return <Card className={styles.chartCard}><h3>Market Section Aggregation</h3><p className={styles.cardSubtitle}>Comparative ticket counts across designated public market zones</p>{query.isLoading ? <div className={styles.loading}>Loading analytics...</div> : query.isError ? <div className={styles.empty}>Unable to load analytics.</div> : !data.length ? <div className={styles.empty}>No data for this date range.</div> : <ResponsiveContainer width="100%" height={250}><BarChart layout="vertical" data={data} margin={{ left: 20 }}><XAxis type="number" allowDecimals={false} /><YAxis type="category" dataKey="section" width={120} /><Tooltip /><Bar dataKey="count" fill="#1e3a8a" radius={[0, 4, 4, 0]} /></BarChart></ResponsiveContainer>}</Card>; }; export default MarketSectionBarChart;
